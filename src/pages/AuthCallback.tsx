
import React from 'react';
import OAuthCallbackHandler from '@/components/destinations/OAuthCallbackHandler';

const AuthCallback: React.FC = () => {
  return <OAuthCallbackHandler />;
};

export default AuthCallback;
